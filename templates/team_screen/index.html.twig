{% extends 'base.html.twig' %}

{% block title %}Hello TeamScreenController!{% endblock %}

{% block body %}

{% include 'first_nav.html.twig' %}

{% if app.user %}
<div class="team-wrapper">
    {% for team in app.user.coaches %}
    <div class="team-card">
        <div class="card-header">
            <a href="">
                <p class="team-cat">{{ team.category }}</p>
            </a>
        </div>
        <div class="card-footer">
            <button class="btn-lg btn-edit pointer" href="">Modifier</button>
            <button class="btn-lg pointer" href="">Supprimer</button>
        </div>
    </div>
    {% endfor %}

</div>
{% endif %}
{% endblock %}