<div class="main-wrapper">

    <!-- <div class="container-fluid"> -->
    <div class="dash-card table-responive">

        {# <div class="card-header"> #}
            {# <h4>Nom</h4> #}
        {# </div> #}

        {# {{ form_start(form) }} #}
        {# {{ form_start(form, {'attr': {'id_player': id_player, 'id_match': id_match  }}) }} #}
		{{ form_start(form, {'action': path('stat_joueur', {'id_player': id_player, 'id_match': id_match  }) }) }}

        <div id="StatMatchForm" class="cell-border row-border">

            <div>{{ form_row(form.rouges) }}</div>
            <div>{{ form_row(form.jaunes) }}</div>
            <div>{{ form_row(form.essais) }}</div>
            <div>{{ form_row(form.transformations) }}</div>
            <div>{{ form_row(form.penalites) }}</div>
            <div>{{ form_row(form.drops) }}</div>


        </div>

        {{ form_end(form)}}

    </div>

</div>
